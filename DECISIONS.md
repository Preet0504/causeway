# Decisions log

A running, day-grouped record of design decisions made on the `causeway-mini` branch, with brief rationale. Forward-only: it starts from the day this file was created and doesn't reconstruct earlier history.

## 2026-09-09

- **Split `bugCap` into `scanCommitLimit` and `bugTarget`.** The single `bugCap` field was asked during `/inspect_repo` (before anything was known to be a bug) but named as if it capped bugs found, which is a real terminology bug, not just a naming preference. `scanCommitLimit` (renamed from `bugCap`) now caps how many commits get scanned in detail; its question in `/inspect_repo` deliberately never mentions "bugs". A new `bugTarget`, asked in `/classify_bugs` once the user knows how many commits were enriched, caps how many accepted results they want.
- **`/classify_bugs` processes commit batches sequentially, newest first, and stops once `bugTarget` is reached**, instead of fanning out every batch of subagents in parallel. Trades the earlier parallel design's speed for fewer subagent calls when the target is smaller than the scanned set. Accepted tradeoff: which commits actually get examined depends on batch order (newest first) rather than which would score best, since there's no way to know that without examining them.
- **Added this decisions log**, `DECISIONS.md` at the repo root, day-grouped entries with brief rationale, forward-only (no retroactive reconstruction of earlier sessions).

## 2026-09-11

- **Removed the early-exit optimization from `InspectRepo`'s window filter.** The user's professor identified a real correctness bug: stopping the walk as soon as one commit older than the cutoff was seen assumed commit timestamps are monotonic along the traversal, which is false for real Git histories (clock skew, rebases, backdated commits, merges of independently-developed branches can all produce a parent with a later timestamp than its own child). The fix visits every commit reachable from HEAD and filters each individually, no shortcut. Traded back some of the walk-speed win from the 2026-09-05 early-exit optimization in exchange for correctness, since a fast wrong answer isn't useful. Extracted the filtering logic into a standalone `findCommitsInWindow` function so it's unit-testable outside the CLI's clone/write machinery.
- **Added a regression test with deliberately skewed commit timestamps** (`InspectRepoWindowSpec`), building a tiny real Git repo by hand where a middle commit is backdated to before its own parent, and asserting the parent (genuinely recent, but "behind" the backdated commit in the walk) is still found. Verified the test actually catches the bug by temporarily reintroducing the old early-exit logic and confirming the test fails there, then confirming it passes again with the fix restored, rather than trusting a passing test without having seen it fail first.
- **Added MUnit (1.1.1) as the project's first test dependency.** No test framework existed yet; MUnit is lightweight and standard for Scala 3, and there was no reason to build anything custom for this.
- **`InspectRepo` now fetches from the remote and pins an explicit repository snapshot on every run, instead of silently reusing whatever a local clone happened to contain.** The professor's follow-up feedback: the repo URL alone does not define repository state, a reused clone with no fetch can produce different results from identical arguments depending only on when it happens to run, and the file's own doc comment claiming determinism was false without something recording what state was actually examined. The evidence file now carries a `repoSnapshot` (`defaultBranch`, `remoteHeadSha`, `retrievedAt`, `usedLocalHeadFallback`), fetched forward through `EnrichCommits` and `/classify_bugs`'s final JSON too, so the whole pipeline's output ties back to one recorded remote state. `findCommitsInWindow` now takes that pinned SHA as an explicit parameter rather than resolving `HEAD` internally, so later operations can't silently drift onto whatever HEAD happens to be at call time.
- **Rejected `refs/remotes/origin/HEAD` as the source of the pinned SHA, uses `repository.getBranch()` (the locally checked-out branch name) plus `refs/remotes/origin/<that branch>` instead.** Tried the symbolic-ref approach first; JGit's `CloneCommand` does not reliably set up `refs/remotes/origin/HEAD` the way plain `git clone` does, so it fell back to local `HEAD`, which is itself never updated by a plain fetch, silently reproducing the exact staleness bug being fixed. Caught this by cross-checking the resolved SHA against GitHub's REST API directly rather than trusting a plausible-looking result.
- **Added a regression test for the fetch-and-pin fix** (`InspectRepoSnapshotSpec`), after being asked directly whether one existed and having to say no, the fetch/pin fix had only been checked by hand against a real GitHub repo. The test uses a local repo as a stand-in remote (no network dependency, so it runs the same everywhere), clones it, pushes a new commit to the stand-in remote, fetches, and asserts `resolveRemoteHead` returns the new commit rather than the one seen at the original clone. Verified it actually catches the bug the same way as the other regression test: temporarily reintroduced the rejected `origin/HEAD` approach, confirmed the test failed, restored the fix, confirmed it passed again.
- **Replaced auto-detection of remote and branch entirely with explicit user choice, discussed after asking "is it possible the selected remote isn't origin" surfaced the fork scenario (a repo's `origin` could be a fork, `upstream` the actual project being studied).** Rather than validating an assumed `origin` against expectations and failing on mismatch, the user proposed listing what's actually there and letting a person pick. `InspectRepo` now runs as four explicit modes (`list-remotes`, `list-branches`, `count`, `write`) instead of one auto-detecting pass. `resolveRemoteHead` and its test (`InspectRepoSnapshotSpec`) were removed, replaced by `listConfiguredRemotes` (tested by `InspectRepoRemotesSpec`, a fork-shaped clone with both `origin` and `upstream` configured, confirming both are surfaced rather than only `origin`) and a GitHub REST API branch listing (`list-branches`) that reports every branch's current SHA directly, so `count`/`write` trust an explicitly chosen SHA rather than resolving anything themselves. The `repoSnapshot` schema changed from `{defaultBranch, remoteHeadSha, retrievedAt, usedLocalHeadFallback}` to `{remoteName, branch, remoteHeadSha, retrievedAt}` to reflect a chosen value rather than a detected one.
- **Branch choice is asked as a plain-text question, not multiple-choice**, even though remote choice uses multiple-choice when there's more than one option. A repo can have many branches (JSON-java has 19), too many for the project's established 2-4-option multiple-choice pattern; the default branch is suggested but the user can type any other name.
- **Built a unified `causeway` CLI, replacing `sbt runMain` as how the chat commands invoke the Scala tools**, per the professor's suggestion that a packaged CLI is a cleaner agent-to-software contract than a build tool's invocation surface, especially as the number of Scala files grows. Discovered along the way that sbt 2's virtual file system, which blocked packaging a plain jar a few days earlier, only virtualizes the *packaged* output: `Compile/dependencyClasspath` returns real, stable paths in the Coursier cache, and compiled `.class` files exist as plain files under `target/out/.../classes`. A classpath doesn't require everything bundled into one jar, so a single `Causeway` main object (dispatching on a subcommand argument to `InspectRepo.run`/`EnrichCommits.run`, both renamed from `main`) plus `tools/causeway`, a launcher that caches that classpath and only re-invokes sbt when a source file or `build.sbt` is newer than the cache, gets the same practical benefit without needing to solve the jar problem at all. Confirmed live: cached invocations run in under a second with zero sbt involvement, versus sbt's several-second build-server round trip every time.
- **The launcher writes Windows-style paths (via `cygpath -w`), not Git Bash's `/c/Users/...` form.** First attempt used `$HOME`-derived MSYS paths directly and failed with `NoClassDefFoundError: scala/runtime/Nothing$`, since `java.exe` is a native Windows binary and Git Bash only translates path-shaped arguments passed literally on a command line, not paths embedded inside a file's contents (which is what a `java @argfile` classpath file is). Caught and fixed by inspecting the generated classpath file directly rather than assuming the failure meant a missing dependency.
- **Added a SQLite repository catalog (`workspace/causeway.db`) as an additive `store` subcommand**, per the professor's suggestion to persist repositories, snapshots, commits, issues, pull requests, issue-commit relations, test observations, bug classifications, materialized revisions, and build runs, each preserving its source/run identifier, with repeated operations upserting rather than duplicating. Kept explicitly additive alongside the existing JSON files rather than replacing them: `/inspect_commits` and `/classify_bugs` each consume the previous stage's JSON file directly and in full, and the JSON files remain the pipeline's actual interchange format. `store` detects which of the three JSON shapes (evidence, enriched, classified) it was given by field presence and upserts accordingly.
- **One table per pipeline stage (`inspect_repo_runs`, `inspect_commits_runs`, `classify_bugs_runs`) instead of one shared `runs` table with a stage/type column.** Matches the professor's own per-concept table style already implied by naming separate tables like `test_observations`, `materialized_revisions`, and `build_runs` rather than one generic table. Also gives downstream foreign keys a precise type: a `bug_classification` always points at a `classify_bugs_run`, never a generically-typed "run" that could be any stage.
- **A single `run_id` (UUID, already minted by `/inspect_repo` and already carried through the JSON files) is shared across all three stage tables of one mining pass**, rather than each stage minting its own. Raised directly by the user: "what if someone runs everything on the same repo but different window and stuff." Each stage table still has its own surrogate autoincrement `id` (used for foreign keys), `run_id` is just a `UNIQUE` column per table, not a cross-table primary key, so the three stage tables don't collide on it.
- **Added `run_commits`, a many-to-many join table between `inspect_repo_runs` and `commits`, instead of a plain column on `commits` recording which run found it.** The same commit can be legitimately rediscovered by a second run with a different window against the same repository (the exact scenario above); a bare column would have the second run's write silently overwrite the first run's membership record. Proven with a regression test (`StoreSpec`, "two different runs against the same repo, different windows") that stores two runs sharing one commit and asserts both runs' `run_commits` rows survive.
- **Author/committer fields on `commits` are upserted with `COALESCE(excluded.col, col)`, not overwritten outright**, because an enriched-file store happens after an evidence-file store for the same commits, and `EnrichCommits` strips author/committer data from its output (an earlier session decision, "these things are useless to me"). Without `COALESCE`, storing the enriched file after the evidence file would null out data the evidence file had already recorded.
- **Fixed a schema-loading bug where the entire `repositories` table definition was silently never created.** `applySchema` originally split `schema.sql` on `;` and filtered out chunks whose *trimmed text started with* `--`; because the file opens with a multi-line comment block directly followed by `CREATE TABLE repositories` with no blank statement between them, both ended up in the same split chunk, and the whole chunk (comment and the `CREATE TABLE` it preceded) got discarded, surfacing later as `no such table: repositories`. Fixed by stripping `--` comment lines individually, line by line, before splitting on `;`, rather than filtering whole semicolon-delimited chunks after the fact.
- **Live-verified `store` against a real evidence file, a real enriched file (from `stleary/JSON-java`), and a hand-built classified file**, reading the resulting `workspace/causeway.db` directly (via `py -c` and the `sqlite3` module, no `sqlite3` CLI available) rather than trusting "no exception thrown." Confirmed idempotency by storing the same evidence file twice and checking row counts stayed flat, and confirmed the PR/issue linking and the author/committer `COALESCE` preservation by inspecting the enriched-store's resulting rows directly.
- **Added `StoreSpec`, proving the run-scoped upsert design with a temp SQLite file per test**: one test stores the same evidence run twice and asserts no duplicate rows anywhere; the other stores two different runs (different `runId`, different window, one shared commit) against the same repository and asserts the repository row is reused, the two runs stay distinct, and `run_commits` correctly reflects both runs' membership for the shared commit. Verified these actually catch a real regression, not just exercise happy-path code, by temporarily removing the `commits` table's `ON CONFLICT` upsert clause, confirming both tests failed with a primary-key constraint violation, then restoring the fix and confirming the full suite (4 tests) passed again.
- **`Store.run` refactored to delegate to a new `private[mini] def storeInto(conn, json, filePath): String`** that takes an already-open `Connection` and returns the detected kind, instead of inlining schema-apply-and-dispatch directly inside `run`'s fixed-database-path, `sys.exit`-on-failure connection-management block. Pure extraction, no behavior change, done specifically so `StoreSpec` could point it at a disposable temp-file database instead of the real `workspace/causeway.db`.
- **Replaced the collapsed `commit_pull_requests` and `issue_commit_relations` tables with one typed `relations` table (a `relation_type` discriminator column, endpoints in nullable `commit_sha`/`pull_request_id`/`issue_id` columns).** The professor's feedback: the pipeline's original commit → PR → closing-issue chain conflated several genuinely different claims (a commit belonging to a PR, that PR closing an issue, a commit's own message merely mentioning an issue number) into one generic "commit relates to issue" fact, when "distinct relationship types" need to be preserved for future bug-fix-evidence confidence scoring. Concretely this had been a real bug: `issue_commit_relations` attributed a PR's closing issue to *every* commit inside that PR, when closing an issue is a fact about the PR as a whole, overstating the connection for every commit in a multi-commit PR except whichever one (if any) actually closed it.
- **Chose one table with a `relation_type` column over a table per relation type**, unlike the per-stage run tables' precedent, because every relation here is the same shape (an edge between two of {commit, PR, issue} plus optional evidence text), and future confidence-scoring needs "every relation touching this commit, of any type" to be one query rather than a UNION across many tables. The per-stage-run-table precedent applied to tables with genuinely different columns per stage; these don't have that problem.
- **The `relations` table's natural-key uniqueness is an expression index (`COALESCE`-ing each endpoint to a non-NULL sentinel), not a plain `UNIQUE` column list.** SQLite treats every `NULL` as distinct for `UNIQUE` purposes, so two `pr_closes_issue` rows for the same PR/issue pair (both with `commit_sha` `NULL`, since that relation has no commit endpoint) would not conflict under a plain `UNIQUE(relation_type, commit_sha, pull_request_id, issue_id)`, silently defeating the whole point of upserting. `ON CONFLICT` targets the same `COALESCE` expressions the index uses.
- **Implemented three of the six relation types identified (`commit_belongs_to_pr`, `pr_closes_issue`, `commit_message_references_issue`) as a first pass, deferring the other three (`commit_mentions_issue`, `pr_references_issue`, `issue_mentions_commit_sha`)**, since those need fetching data the pipeline doesn't fetch today (an issue's own timeline events and body/comment text, not just the existing commit → PR → issue chain). Asked the user directly rather than assuming; they agreed to this staged scope, and to one shared table over one table per type.
- **`commit_message_references_issue` is a plain `#(\d+)` regex over the commit's own message, deliberately not filtered or cross-checked against whether the number is really an issue** (as opposed to a PR, or nothing at all). This is intentional: a merge commit's own boilerplate "Merge pull request #1072 from ..." message will match its own PR number here too, which is exactly why this is kept as its own distinct, *weaker*, unconfirmed-by-GitHub relation type rather than folded into the stronger, GitHub-resolved `pr_closes_issue` or a future GitHub-confirmed `commit_mentions_issue`. Live-verified against `stleary/JSON-java`: the merge commit `4f859fdf3b...` correctly picked up both its own PR number (1072) and the real issue number (1071) as raw mentions, alongside a separate, correctly-scoped `pr_closes_issue` row (PR 1, issue 1071, no commit attached).
- **Extended `StoreSpec` with three more regression tests** covering the exact bug being fixed (a two-commit PR with one closing issue must produce 2 `commit_belongs_to_pr` rows but only 1 `pr_closes_issue` row, with no `commit_sha`), the new raw-text relation type, and idempotency of `relations` rows on a repeat store. Verified the PR-closing test actually catches the regression it exists for: temporarily restored the old per-commit attribution (passing `commitSha = Some(sha)` into the `pr_closes_issue` upsert), confirmed the test failed with `2` instead of the expected `1`, then restored the fix and confirmed the full 7-test suite passed again.
- **Added repository discovery: `SearchRepos.scala` (a `search-repos` subcommand), a `repo-discovery` subagent, and a `/discover_repos` chat command**, per the professor's feedback that a discovery agent should transform plain-language requirements into a structured search specification and invoke the search CLI, without itself touching `curl`, git, the database, bug classification, or builds. Kept the same division of labor as `bug-classifier`/`/classify_bugs`: the agent does the fuzzy, semantic translation (a person's plain-language request into structured flags), the compiled tool does the deterministic, mechanical work (querying GitHub's search API, paginating, writing to SQLite). The `repo-discovery` agent's frontmatter restricts it to the `Bash` tool only, and its instructions additionally spell out, explicitly, that Bash is for invoking `tools/causeway search-repos` and nothing else, defense in depth rather than relying on tool-restriction alone.
- **`search-repos` writes to `workspace/causeway.db` directly as it paginates, unlike the other three pipeline stages, which write a JSON file and go through a separate `store` step.** Asked the user directly rather than assuming, since this is a real architectural fork; the professor's own phrasing ("inserted into the database immediately") plus the fact that nothing downstream needs to read a search-repos JSON file (unlike evidence/enriched/classified files, which the next stage always reads in full) both pointed the same way. `Store.applySchema`, `Store.upsertRepository`, `Store.exec`, and `Store.queryId` were widened from `private` to `private[mini]` so `SearchRepos` could reuse them directly rather than duplicating schema application and the repositories upsert a second time.
- **Added `search_repos_runs` (the exact search specification, mirroring `inspect_repo_runs`'s provenance role) and `search_run_repositories` (a join table linking a search to what it found, mirroring `run_commits`'s "the same thing can be rediscovered by a different run" design).** A discovered repository's stars/language/last-pushed-date/etc. are stored per search (on the join row), not on the stable `repositories` identity row, since that metadata changes over time and a repository found by two searches months apart should keep each search's own point-in-time snapshot rather than one overwriting the other.
- **"Recent activity" is asked for as a duration (`--pushed-within-months N`), resolved to an absolute date internally**, matching `/inspect_repo`'s existing "past 1/3/6 months" window UX rather than introducing a second, inconsistent way of expressing a time period elsewhere in the same pipeline. Both the requested duration and the resolved date are stored on `search_repos_runs`.
- **Topics are accepted as one comma-separated `--topics` flag (not a repeated `--topic` flag) and stored as a single comma-joined column**, matched together: multiple topics are AND'ed in one GitHub search qualifier string (`topic:a topic:b`), and since they're part of one search's own specification rather than a reusable dimension queried across searches, a further-normalized join table would add a table without adding real query power.
- **`--max-results` is required, with no silent default, and is capped at GitHub's own hard ceiling (1000 results per query, 100 per page × 10 pages) rather than trusting whatever number is asked for.** Matches the existing `scanCommitLimit`/`bugTarget` pattern of never silently defaulting a limit that meaningfully shapes what gets returned. A request above the ceiling is honored up to the ceiling with a `WARN` on stderr, not silently truncated without saying so.
- **Live-verified against the real GitHub search API** (`language:Java stars:>=5000 pushed-within-months:6 archived:false fork:false license:mit`, 5 results), then inspected `workspace/causeway.db` directly to confirm `search_repos_runs`, `search_run_repositories`, and `repositories` all populated correctly, not just that the tool exited without error.
- **Added `SearchReposSpec`**, testing the pure qualifier-building logic (`buildQualifiers`, including the empty-criteria and size-only-bound cases) and the database-writing guarantees directly (`upsertSearchRepoRun`/`storeDiscoveredRepo` widened to `private[mini]` for this, the same pattern used for `Store.storeInto`): storing the same discovered repo twice under one search doesn't duplicate rows, and the same repository discovered by two different searches keeps each search's own snapshot rather than sharing or overwriting one. Verified the idempotency test actually catches a regression: temporarily removed the `search_run_repositories` upsert's `ON CONFLICT` clause, confirmed the test failed with a primary-key violation, restored the fix, confirmed the full 12-test suite passed again.
- **`/inspect_repo` now handles a repo URL that's already known but with no window chosen yet** (the shape `/discover_repos` leaves a session in), asking only for the window instead of falling through to its generic "ask for both from scratch" path and re-asking for a URL that was already picked.
- **Added cheap pre-clone repository qualification: `QualifyRepos.scala` (a `qualify-repos` subcommand) and a `repository_qualifications` table**, per the professor's feedback that discovered repositories should be checked for likely usefulness (issues enabled, candidate issues/PRs existing, likely test presence) before anything clones them, since cloning is the expensive step in this whole pipeline. Three cheap GitHub calls per repository: `has_issues` from repo metadata, two `search/issues` calls for closed-issue and merged-PR existence counts (only `total_count` is read, not the items), and one recursive git-tree listing (paths only, no file content, no clone) pattern-matched against common test-file conventions.
- **Two hard disqualifiers, one soft signal**: `has_issues: false`, or zero closed issues *and* zero merged PRs, both mean this repository could never produce the commit-to-issue evidence this pipeline looks for, so both reject outright. Whether the tree appears to contain tests is recorded (`appears_to_have_tests`, plus a handful of matched sample paths) but never on its own fails a repository, it's a plain regex-pattern heuristic that can easily miss real tests living in an unconventional location or an unrecognized language. Asked the user directly rather than assuming which signals should be hard-disqualifying.
- **`repository_qualifications` is one current row per repository (`repository_id` as its primary key), re-checking replaces it, unlike `search_run_repositories`'s deliberately-preserved per-search history.** Qualification answers "is this worth cloning right now", nothing depends on reproducing a stale answer from an earlier check the way reproducing a past search's exact result set matters; keeping only the latest check avoids the join-table's find-the-current-row complexity for a case where only the current answer is ever actually useful.
- **Qualification is invoked directly by `/discover_repos` itself, not routed through the `repo-discovery` agent**, and runs automatically right after search, before any results are shown. Unlike translating "popular, actively-maintained Java repos" into search flags, qualifying a repository against fixed thresholds is a deterministic function of GitHub metadata with no judgment call for an LLM to make, so it doesn't belong behind an agent at all; keeping it in the command also keeps the `repo-discovery` agent's stated boundary (search only, no other database or GitHub interaction) intact rather than widening it. Running it automatically, rather than as a separate manual step, means a person is never shown a raw candidate list without also knowing which ones are worth pursuing.
- **Live-verified against real GitHub repositories**: `stleary/JSON-java`-adjacent popular Java repos (from an earlier real search) all qualified correctly with real closed-issue/merged-PR counts and detected test paths; `torvalds/linux` (which famously disables GitHub issues in favor of a mailing list) was correctly rejected with `reason="issues are disabled on this repository"`, confirmed via a direct `--owner/--repo` qualify-repos call, not just plausible-looking output.
- **Added `QualifyReposSpec`**, testing the pure decision logic directly (`decideQualification`'s two hard-disqualifier cases and the "either signal alone is enough to qualify" case, `detectTestEvidence` against paths from several language conventions and against a tree with no test-like paths at all) and that re-qualifying a repository replaces its one row rather than duplicating it (`upsertQualification` widened to `private[mini]` for this, the same pattern used throughout). Verified the replace-not-duplicate test actually catches a regression: temporarily removed its `ON CONFLICT` clause, confirmed a primary-key violation, restored the fix, confirmed the full 19-test suite passed again.
- **Implemented the three previously-deferred relation types (`commit_mentions_issue`, `pr_mentions_issue`, `issue_mentions_commit`) and split the commit↔PR edge into two distinct relations (`commit_associated_pr`, `pr_contains_commit`) instead of one, after the professor named all six explicitly and asked not to reduce them to one generic linked-issue field.** Checked our own claim of being "done" first, rather than assuming: found that two of the three previously-implemented relation types (`commit_belongs_to_pr`, `pr_closes_issue`) had `evidence` stored as `NULL`, not satisfying "store the source text or GitHub object that establishes every relation" the professor now stated explicitly, and that the request's `commit_associated_pr` and `pr_contains_commit` were both being served by one relation (`commit_belongs_to_pr`). Asked directly whether those two were meant to be the same edge or genuinely different, since GitHub's `Commit.associatedPullRequests` (which commit a PR is resolved as belonging to) and a `PullRequest`'s own `commits` list (which shas are still literally in that PR's diff) are two separately-sourced facts that can disagree, e.g. after a rebase drops a commit from a PR without un-resolving the association. Confirmed: two distinct relations, not a rename.
- **`EnrichCommits.scala` now fetches two things it didn't before**: each associated PR's own `commits(first: 100)` list (needed for `pr_contains_commit`), and, in a second batched GraphQL pass over every issue discovered via `closingIssuesReferences`, that issue's `body` text and `timelineItems` (`ReferencedEvent` for `commit_mentions_issue`, `CrossReferencedEvent` with a PR source for `pr_mentions_issue`). `EnrichCommits` only fetches this raw data now, as before, `Store.storeEnriched` is what turns it into typed relations, keeping the fetch/derive split already established.
- **Fixed a latent overwrite bug in `upsertIssue` and `upsertPullRequest`, found while adding call sites that only have partial data (a number with no title, or a title with no state).** Both previously did a plain `title = excluded.title` (etc.) on conflict, never `COALESCE`, unlike `upsertCommit`. In practice this hadn't caused visible damage yet because every existing call site always passed full data, but the new `issue_mentions_commit`/`commit_message_references_issue` call sites (number only) and the new `pr_mentions_issue` call site (number/title/url, no state) would have silently nulled out real data recorded earlier by a richer call for the same row. Fixed both to `COALESCE(excluded.col, col)`, matching `upsertCommit`'s existing pattern, before it could actually bite.
- **Live-verified against `stleary/JSON-java`'s real PR #1072/issue #1071**, and found a genuine real-world confirmation of exactly the divergence the two commit↔PR relations exist for: both of the enriched run's merge commits got a `commit_associated_pr` row but no matching `pr_contains_commit` row, because GitHub resolves a merge commit as associated with the PR it merged, but that PR's own commit list only contains the commits merged in, not the merge commit itself. Also observed a real cross-repository `ReferencedEvent` (a commit in a contributor's fork referencing the upstream issue), correctly flagged in `commit_mentions_issue`'s evidence text.
- **Extended `StoreSpec` to 24 tests total**: renamed the `commit_belongs_to_pr` assertions to `commit_associated_pr`, and added coverage for the `commit_associated_pr`/`pr_contains_commit` divergence, the foreign-key safety guard on `commit_mentions_issue` (a referenced commit outside the scanned set must not be linked), `pr_mentions_issue` staying distinct from `pr_closes_issue`, and `issue_mentions_commit`'s prefix-matching against a short SHA pasted in issue text. Verified both new safety-critical tests actually catch what they're meant to: temporarily let `pr_contains_commit` fire unconditionally (test failed, 1 instead of 0), and temporarily removed the "only link commits this run actually knows about" guard on `commit_mentions_issue` (test failed, 1 instead of 0), then restored both and confirmed the full 24-test suite passed again.
- **Centralized GitHub access (both the network call and the `GITHUB_TOKEN` credential) into the Scala CLI layer, per the professor's feedback that it had fragmented across the workflow**: `run_causeway.md` ran its own raw `curl` to validate a repo URL, and `inspect_commits.md` separately instructed the shell to source `.env` so `GITHUB_TOKEN` would be set, two different command files each independently responsible for a piece of network/credential handling that has nothing to do with what those commands are actually orchestrating. `tools/causeway` (the launcher) now sources `.env` itself, before invoking `java` at all, so every subcommand's own process environment already has `GITHUB_TOKEN` without any chat command needing to prepare it. Added a fifth `InspectRepo` mode, `validate --repo-url <url>` (prints `VALID=true`/`VALID=false`, clones nothing), replacing `run_causeway.md`'s and `inspect_repo.md`'s own `curl` calls.
- **Made `InspectRepo.scala`'s own GitHub REST calls authenticated, which they hadn't been before.** `list-branches`/`fetchDefaultBranch`/`fetchBranches` previously sent no `Authorization` header, relying on GitHub's much lower unauthenticated rate limit, because until now nothing guaranteed `GITHUB_TOKEN` would actually be set when `/inspect_repo` ran (only `/inspect_commits` explicitly sourced `.env`). Now that the launcher guarantees it for every subcommand, `InspectRepo` requires and uses `GITHUB_TOKEN` the same way `EnrichCommits`/`SearchRepos`/`QualifyRepos` already did, for a real (not just architectural) reliability improvement.
- **Live-verified the launcher's `.env` sourcing directly**: confirmed `GITHUB_TOKEN` was absent from the shell's own environment before invoking `tools/causeway inspect-repo --mode validate`, then confirmed the call still succeeded (`VALID=true` for a real repo, `VALID=false` for a nonexistent one), proving the launcher's own sourcing is what supplied the credential, not something left over in the shell. Also regression-checked `list-remotes`/`list-branches` against `stleary/JSON-java` to confirm the newly-authenticated calls still behave identically to before.

## 2026-09-12

- **Every question a command asks now defaults to the AskUserQuestion tool (real clickable multiple-choice), including numeric caps that were previously considered free-text-only** (scan commit limit, bug target, max search results), per the user's explicit standing preference: "wherever we are asking questions to user, let us make them multiple choice questions so the user can get a better idea on what he needs to answer." Numeric questions get 2-4 preset options scaled to whatever context is already known (e.g. roughly a fifth/half/all of a shown commit count), never a fixed list that might not fit the actual numbers, and never a manually-added "Other" option, the tool already offers a custom-answer escape hatch automatically for anything that doesn't match a preset.
- **Confirmed via a clarifying round, since AskUserQuestion caps out at 2-4 options, which values genuinely can't become multiple-choice**: a repo URL, `/discover_repos`'s open-ended "what kind of repo" description, and a repo's full branch list when it exceeds 4. Branch choice now shows the default plus a few relevant others as options (mentioning more exist) rather than the old plain-text-only approach, still relying on the tool's custom-answer for anything not shown, rather than trying to enumerate every branch.
- **File-selection questions (which evidence/enriched file to use, when more than one exists) also converted to AskUserQuestion**, one option per file (most recent first, capped at 4), for the same reason: a person picking from a visible small set of files is exactly the case this tool is for, not a case that should stay a typed path.
- **Clarified that the `repo-discovery` agent (tool-restricted to `Bash` only) has no way to ask the user anything directly** — its existing "ask" language in `.claude/agents/repo-discovery.md` was aspirational, since it isn't an interactive conversation with the end user, only a result returned to whatever invoked it. Reworded to "report back what's missing"; `/discover_repos` (which does have full tool access) is what actually calls AskUserQuestion and re-invokes the agent once it has an answer, tightened explicitly in `discover_repos.md` step 2.
- **Updated the `causeway-mini-onboarding-ux-pattern` memory** to record this as a refinement of the existing UX standing practice (superseding its earlier "numeric caps are fine as free text" guidance), not a one-off change, so future work on these commands defaults to it without being asked again.
- **`/discover_repos`'s open-ended "what kind of repository" question now shows a few complete example answers** (e.g. "popular Java web frameworks that are still actively maintained") instead of just a list of dimensions to consider (language, stars, activity, ...), after noticing the dimension list alone doesn't actually show what a valid answer looks like. Kept as free text either way, per the AskUserQuestion-can't-fit-this-many-options constraint above, this only changed what the question shows, not the mechanism.
- **Removed the three stub tables (`test_observations`, `materialized_revisions`, `build_runs`)** from `schema.sql`, at the user's request to drop tables not being used at this point. Confirmed via grep first that no Scala code reads or writes any of them, only the schema comments referenced them. They were part of the professor's originally-stated minimum schema (for future `detect-tests`/`materialize-commit`/`build` capabilities that don't exist yet), so this is a deliberate, easily-reversible deferral, not an oversight, add them back individually whenever the capability that actually needs one gets built.
- **Cleared `workspace/causeway.db`** (a gitignored, local runtime artifact, confirmed via `git status --ignored` before deleting), at the user's request, and regenerated a fresh one to verify the trimmed schema still applies cleanly (15 real tables plus SQLite's own `sqlite_sequence`, down from 18), then cleared that verification data too so the database starts genuinely empty rather than seeded with a test search's results.
- **Added `/list_qualifying_repos` (a new `list-qualifying-repos` CLI subcommand plus `ListQualifyingRepos.scala`), a pure database read surfacing every currently-qualifying repository ever discovered, not just the most recent search's results.** Raised by asking "is there any point in storing repos we don't mine" — the honest answer at the time was that nothing read them back, so this closes that gap and makes the stored-but-unused catalog data actually useful. Confirmed directly with the user first that database access should follow the exact same rule GitHub access already does: the CLI owns it, a command file must never run its own SQL, matching the `curl`-centralization precedent from two days earlier.
- **Scope resolved as "every qualifying repo, ranked, capped by a user-supplied `--limit`" rather than "only the most recent search" or "unbounded"**, the user's own proposed resolution to the scope question, after `--limit` was flagged as needing to be required (no silent default) to match the existing `scanCommitLimit`/`bugTarget`/`max-results` convention. Ordered by each repository's most recent discovery's star count (via a `ROW_NUMBER() OVER (PARTITION BY repository_id ORDER BY created_at DESC)` window function against `search_run_repositories`/`search_repos_runs`, SQLite window functions confirmed supported since sqlite-jdbc 3.49.1.0 bundles a far newer SQLite than the 3.25 baseline), so a repo discovered by two searches shows one row with its freshest known snapshot, not a duplicate or a stale one.
- **Each listed repository is flagged with whether it's already been mined** (an `EXISTS` check against `inspect_repo_runs.repository_id`), confirmed as wanted since it's what actually lets someone tell "still just a candidate" apart from "already looked at this" when scrolling a catalog built up over many searches.
- **Live-verified against a real search** (`language:Java stars:>=20000`, 5 real repos, all qualifying): `list-qualifying-repos --limit 3` correctly returned the top 3 by stars with `alreadyMined=false` for all, and `TOTAL_QUALIFYING=5` correctly reflected the true total beyond the 3 shown, then cleared that verification data afterward.
- **Added `ListQualifyingReposSpec`** (5 tests: non-qualifying repos excluded, star-descending ordering with `--limit` respected, the two-searches-one-repo recency case, `alreadyMined` correctness, and total-vs-shown count independence). Verified the recency test actually catches a regression: temporarily flipped the window function's `ORDER BY ... DESC` to `ASC`, confirmed the test failed by returning the stale snapshot (`100` instead of `999`), restored the fix, confirmed the full 29-test suite passed again. Also fixed a real bug caught while writing this file, not by a test: an initial draft called `rs.wasNull()` after already reading a second column, when JDBC's `wasNull()` only reflects the most recently read column, silently checking the wrong column's null-ness.
- **A schema-wide brainstorming pass, prompted by four separate observations: commit author/committer fields looked unused, qualification output showed only booleans where richer counts would be more useful, `search_run_repositories`/`repository_qualifications`/`repositories` looked like they might be redundant, and small sequential IDs "didn't seem professional."** Resolved each on its own merits rather than as one bundled change, confirmed via a 4-question round before touching anything:
  - **Dropped `author`/`committer` (name, email, date, and the committer equivalents) entirely, from both the evidence JSON and the `commits` table.** Checked first: nothing had ever read these columns back for any computation, they were stored purely because JGit hands them over for free alongside data that *is* used. `commitDate` (the commit's own timestamp, not tied to a person's identity) was kept, it wasn't part of what was asked about and remains a plain, still-possibly-useful fact.
  - **Folded `repository_qualifications` into `repositories` as nullable columns, removing the table entirely.** The tell: `repository_qualifications.repository_id` was itself that table's primary key, a strict one-to-one relationship with `repositories`, not a genuine one-to-many one, exactly the shape nullable columns on a base table are for. `QualifyRepos.upsertQualification` changed from an upsert-into-a-join-table to a plain `UPDATE repositories ... WHERE id = ?`; `ListQualifyingRepos`'s query lost a join entirely. `search_run_repositories` was kept separate on purpose, it's genuinely one-to-many (a repo can be discovered by many searches) with different retention semantics (full history, not "latest only"), the redundancy concern didn't actually apply there once checked field-by-field.
  - **Added `open_issue_count` and `open_pr_count` alongside the existing `closed_issue_count`/`merged_pr_count`** (two more `search/issues` calls, four total instead of two), and replaced the `appears_to_have_tests` boolean with a real `test_file_count` (the true, uncapped count from `detectTestEvidence`, previously only its `.nonEmpty` was exposed and the count itself thrown away). `has_issues` was kept as its own boolean, deliberately not folded into a count, since a repo can have issues enabled with zero ever filed, a different fact than disabled, that a count alone can't distinguish. None of the new counts affect `qualifies` itself, `decideQualification`'s two hard disqualifiers are unchanged.
  - **Kept sequential integer surrogate keys as-is.** For a local, single-writer SQLite file nobody queries directly from outside this tool, small integers are the more efficient, conventional choice (smaller/faster indexes and joins); the "looks unprofessional" concern mainly applies to public APIs that expose IDs to outsiders and leak volume/insertion-order information, which doesn't apply here. Asked directly whether something else was behind the concern rather than assuming; confirmed there wasn't.
  - **`qualify-repos` and `list-qualifying-repos`'s printed output and rendered tables now show the actual counts** (`closedIssues=... openIssues=... mergedPrs=... openPrs=... testFiles=...`) instead of just a boolean `qualifies` verdict, in both `/discover_repos`'s and `/list_qualifying_repos`'s result tables.
  - Live-verified the whole redesign end to end against real repositories (`krahets/hello-algo`, `iluwatar/java-design-patterns`, `stleary/JSON-java`): confirmed the richer counts populate correctly, confirmed a repository discovered via `/inspect_repo` but never qualified correctly shows `NULL` for every qualification column (proving they're genuinely optional, not silently defaulted), confirmed the evidence JSON's commit objects no longer carry `author`/`committer` at all, and confirmed the full 29-test suite (after updating `QualifyReposSpec`, `ListQualifyingReposSpec`, and `StoreSpec`'s fixtures for the new shapes) still passes.
- **Added `--max-stars`, a real gap the user spotted: `--min-size-kb`/`--max-size-kb` already supported a full range, but stars only ever had a lower bound.** Not a design choice, an oversight, fixed to mirror the existing size-range pattern exactly: `buildQualifiers` now takes `(minStars, maxStars)` and emits `stars:MIN..MAX` / `stars:>=MIN` / `stars:<=MAX` the same way size already did. Added `max_stars` to `search_repos_runs` and to the `repo-discovery` agent's flag list (`--min-stars`/`--max-stars` together handle "not too obscure but not enormous either" style requirements). Live-verified: `--min-stars 5000 --max-stars 6000` returned only repos with 5998-6000 stars, confirmed the upper bound is actually enforced, not just accepted and ignored.
- **Added repository size to every rendered repo table** (`/discover_repos` and `/list_qualifying_repos`), at the user's request, formatted human-readably (KB under 1024, MB with one decimal place above that) rather than a raw KB number. The underlying data (`size_kb`) was already being fetched and stored by `search-repos`, just never surfaced in its own `REPO` output line or read back by `list-qualifying-repos`'s query, both fixed to carry it through. `ListQualifyingReposSpec` extended with a test confirming size follows the same most-recent-search recency rule as stars (a repo discovered twice shows the newer search's size, not a stale or duplicated one), and `linkDiscovery`'s test helper gained a `sizeKb` parameter (defaulted, so existing call sites needed no changes). Live-verified size renders correctly end to end (e.g. `krahets/hello-algo` at 478,300 KB / ~467 MB).
- **Removed the `qualifies` boolean column entirely.** The user's observation: it's redundant with `rejection_reason`'s own nullability, `qualifies` was always exactly `rejection_reason IS NULL`, a persisted column that could never actually disagree with the reason it's paired with is pure duplication, not two independent facts. "Qualifies" is now purely a query condition, computed wherever needed, never stored. Caught and fixed a real correctness subtlety while making this change, not something asked for but a genuine bug the change would otherwise have introduced: a repository discovered by `search-repos` but never run through `qualify-repos` also has `rejection_reason = NULL` (for an entirely different reason, never checked, not checked-and-passed), so `rejection_reason IS NULL` alone would have wrongly counted an unchecked repository as qualifying. Fixed by also requiring `checked_at IS NOT NULL` everywhere the old `qualifies = 1` condition used to be. Added a regression test for exactly this case and verified it actually catches the bug: temporarily removed the `checked_at IS NOT NULL` guard, confirmed the test failed (a never-checked repository wrongly appeared as qualifying, count `1` instead of `0`), restored the fix, confirmed both affected spec files (13 tests) passed again. `QualificationResult.qualifies` stays as an in-memory Scala field for convenience (it's a fresh, always-correctly-paired computation from `decideQualification`, not a persisted fact that could drift), only the database column was removed.
- **Captured more of what the qualify/search API calls already return for free, after being asked "what else metadata can we get in the same round trip."** Inspected the real `GET /repos/{owner}/{repo}` response directly (a live `curl`) rather than guessing at its shape, and found `search-repos`'s own search result items carry the identical fields. Split the additions into two kinds, confirmed via a 2-question round rather than assumed:
  - **Stable identity facts** (`description`, `repo_created_at`, `forks_count`, `topics`, `default_branch`) added as plain nullable columns on `repositories`, populated by whichever tool (`search-repos` or `qualify-repos`) happens to see a repository first. `Store.upsertRepository` gained optional parameters for all five, `COALESCE`-preserved on conflict (mirroring the `upsertIssue`/`upsertPullRequest` pattern already established), so search finding a repo first and qualify checking it later (or the reverse, for a directly-qualified repo) never lets the second call null out what the first already recorded.
  - **`qualify-repos`'s own point-in-time snapshot** (`current_stars`, `current_language`, `current_size_kb`, `current_archived`, `current_fork`, `current_license`) added as separate columns, deliberately distinct from `search_run_repositories`' per-search historical snapshot (not the same redundancy the `qualifies` column had: these are genuinely two different concepts, point-in-time-of-discovery vs. point-in-time-of-last-check). Closes a real gap: a repository qualified directly (`--owner`/`--repo`, bypassing search entirely) previously had no stars, language, size, archived, fork, or license data recorded anywhere at all.
  - `default_branch` specifically was already being fetched by `qualify-repos` (needed for the tree call) and silently discarded every time, now persisted at zero extra API cost.
  - Live-verified end to end against `krahets/hello-algo`: confirmed `search-repos` correctly populated the real (Chinese-language) description, creation date, fork count, and comma-joined topics; confirmed `qualify-repos` correctly populated its own current-snapshot columns (`current_stars=130035`, `current_license='NOASSERTION'`, etc.) independently.
- **Added `repo-details` (a new CLI subcommand plus `RepoDetails.scala`), a one-repository lookup, after being asked directly "so when do we actually show these details to user" for the newly-captured metadata.** Honest answer at the time: nowhere, the new columns weren't even reaching the CLI's own stdout yet. Confirmed the actual design fork first rather than guessing: the result tables in `/discover_repos` and `/list_qualifying_repos` are already fairly wide (repo/stars/language/size/issues/PRs/test files/mined/url), so cramming in description/topics/license/creation-date/archived-fork status as more columns would make them unreadable. Resolved as a separate, one-repo "detail view" shown only for the repository someone actually picks to mine (in both commands' "offer to continue" step), not as extra columns shown for every candidate — matches the same CLI-owns-database-access rule as `list-qualifying-repos`, a command file still never runs its own SQL. Live-verified against `krahets/hello-algo`: correctly showed identity fields (topics, forks count, default branch, creation date) even before qualification, and correctly showed the `current_*` snapshot fields only after `qualify-repos` had actually checked it. Added `RepoDetailsSpec` (3 tests: unknown repo returns `None`, identity fields available pre-qualification, current-snapshot fields available post-qualification).
- **A follow-up redundancy/optimization audit of the whole schema, requested directly ("check for redundancy in our database, if something can be optimized"), turned up five findings; the user asked for a concrete fix per finding, reviewed all five, and approved every one before any code changed.**
  - **Fix 1 (the significant one): unified `repositories.current_*` and `search_run_repositories` into one source of truth for "what does this repository currently look like."** Before this, `qualify-repos` wrote `current_stars`/etc. straight from `upsertQualification`, while `search-repos` wrote its own snapshot only to `search_run_repositories`, read back only by `list-qualifying-repos` (confirmed via grep that it never referenced `current_stars` at all) — two unreconciled sources of truth for the same fact, meaning the results table and the one-repo detail view (`repo-details`, which only ever read `current_*`) could show two different numbers for the same repository. Fixed by extending `Store.upsertRepository` with the same six `current_*` parameters, `COALESCE`-preserved exactly like the existing identity fields, and having `search-repos` pass its already-extracted stars/language/size/fork/archived/license into that same call (no new API cost, the values were already being read for the `search_run_repositories` insert). Removed the now-redundant `current_*` `SET` clauses from `upsertQualification` (it keeps only the qualification-specific columns), and simplified `ListQualifyingRepos.fetchTopQualifying`'s SQL to read `repositories.current_*` directly, deleting the `ROW_NUMBER() OVER (PARTITION BY repository_id ORDER BY search_repos_runs.created_at DESC)` window-function join entirely — a repository discovered by two searches now shows one row because there's only one `current_*` per repository, not because a query picked the newest of several. `search_run_repositories` itself is untouched and still keeps its own per-search historical snapshot, only its use for "current" data was removed. Rewrote `ListQualifyingReposSpec`'s two recency-dependent tests (previously driven by inserting distinct `search_repos_runs.created_at` timestamps) to instead call `Store.upsertRepository` directly with different `current_*` values and assert the later call's values win, matching the new "last write wins via `COALESCE`" mechanism rather than the old "most recent search timestamp" one. Extended `RepoDetailsSpec` with a test that deliberately passes *mismatched* `current_*` values into `upsertQualification` after `Store.upsertRepository` already set the real ones, asserting `repo-details` still shows the original values, i.e. that `upsertQualification` no longer touches these columns at all; verified this test actually catches the old behavior by temporarily re-adding the `current_*` `SET` clauses back into `upsertQualification`, confirming the test failed (`9999` instead of `1234`), then restoring the fix and confirming it passed again.
  - **Fix 2: dropped `classify_bugs_runs.scanned_commit_count`.** It was always identical to `inspect_commits_runs.enriched_commit_count` for the run being classified (classify-bugs scans exactly what enrich-commits produced, nothing more), a persisted duplicate of a fact already recorded one table over. The evidence chain (`inspect_commits_run_id` foreign key) already lets anyone needing that count join to get it. The classified JSON output itself still carries `scannedCommitCount` (the LLM-facing artifact, `.claude/commands/classify_bugs.md`, was left untouched), only the redundant database column and the code path that persisted it were removed.
  - **Fix 3: dropped the `commit_parents` table entirely.** Confirmed via grep first that nothing in the codebase ever read a `parent_sha`/`parent_order` value back for any computation, it was stored purely because JGit hands parent SHAs over for free alongside data that is used, the same shape of oversight as the earlier author/committer removal. The evidence JSON's own `parentShas` field is untouched (still the primary artifact, still written by `InspectRepo`), only the pointless downstream database write (and the `upsertCommitParent` function itself) were removed.
  - **Fix 4: renamed `search_repos_runs.topics` to `topic_filter`.** The old name collided in meaning with `repositories.topics` (a repository's own actual topics) despite being a completely different fact (what topic a search filtered *on*), a naming collision the user's original "id's just simple 1,2,3 which doesn't seem professional" schema-cleanliness concern would have flagged too, once actually looked at side by side. Renamed the schema column and the `Scala` parameter/SQL together (`SearchRepos.upsertSearchRepoRun`); the CLI's own `--topics` flag name is unaffected, since that's a user-facing search discriminator, not the internal column.
  - **Fix 5: added plain B-tree indexes on `relations(commit_sha)`, `relations(pull_request_id)`, and `relations(issue_id)`.** `relations_natural_key` (the existing uniqueness guarantee) is a `COALESCE`-wrapped *expression* index, which does not serve a plain `WHERE commit_sha = ?` lookup, the exact access pattern used whenever anything walks from a commit/PR/issue to what it's connected to. No query code changed, this is a pure lookup-performance addition with no behavioral effect, verified by re-running the full targeted test batch afterward with no changes needed.
  - Compiled cleanly after each fix; ran the five directly-affected spec files (`QualifyReposSpec`, `SearchReposSpec`, `RepoDetailsSpec`, `ListQualifyingReposSpec`, `StoreSpec`, 32 tests total) rather than the full suite, per the user's standing "you don't need to execute full suite for this small changes" preference — none of the five fixes touch `InspectRepoRemotesSpec`/`InspectRepoWindowSpec`'s code paths (confirmed via grep before skipping them).
  - The user separately asked, in the same reply approving all five fixes, for `search-repos` to expose "more flags that actually matter" now that so much repository metadata is captured for free — deliberately treated as a distinct, not-yet-scoped follow-up (which specific flags) rather than folded into this audit, since scope wasn't given and guessing risks building the wrong ones.
- **Scoped the "more discovery flags" follow-up by recommending three candidates, ranked by how often they'd show up in a plain-language request and how cleanly they map onto metadata already captured** (fork count and creation date are already stored fields; an org/user scope isn't, but is a cheap GitHub qualifier); the user picked two of the three, forks and repo age, explicitly deferring `--org`.
  - **Added `--min-forks`/`--max-forks`**, mirroring the existing `--min-stars`/`--max-stars`/size-range pattern exactly (`forks:MIN..MAX` / `forks:>=MIN` / `forks:<=MAX`), since `forks_count` was already being captured but had no way to filter a search on it.
  - **Added `--min-repo-age-years`/`--max-repo-age-years`**, mirroring `--pushed-within-months`'s "ask for a duration, resolve to an absolute date, store both" pattern, but applied to `repo_created_at` (creation date) instead of last-activity date. Age and date move in opposite directions, a genuinely different shape from every other range flag in this tool: a *larger* minimum age means an *earlier* (smaller) creation-date cutoff (`created:<=date`, stored as `created_before_date`), and a *larger* maximum age means a *later* (larger) cutoff (`created:>=date`, stored as `created_after_date`). Worked the direction out from a concrete example first ("at least 2 years old" means "created on or before 2 years ago," not "after"), then wrote a test for each direction explicitly rather than trusting the symmetry with the other range flags to carry over automatically.
  - Added six columns to `search_repos_runs` (`min_forks`, `max_forks`, `min_repo_age_years`, `max_repo_age_years`, `created_before_date`, `created_after_date`), matching the existing provenance pattern of storing both the requested value and any resolved value, not just one.
  - Extended `buildQualifiers`'s test coverage with the full-discriminator case plus one test each for a forks-only lower bound and a repo-age-only bound in each direction (confirming the age-inversion direction concretely: `--max-repo-age-years` produces `created:>=`, `--min-repo-age-years` produces `created:<=`), and updated the two `upsertSearchRepoRun` test call sites for the new parameters. Ran only `SearchReposSpec` (9 tests) afterward, not the full suite, per the user's repeated "you don't need to execute full suite for this small changes" instruction — none of the other spec files touch `search_repos_runs` or `buildQualifiers`.
  - Updated the `repo-discovery` agent's flag documentation and `/discover_repos`'s dimension list to mention fork count and repo age as recognizable requirement dimensions, and clarified in the agent's docs that `--min-forks`/`--max-forks` (how many times *other* repos fork this one) is a distinct concept from `--fork` (whether this repository is itself a fork of something else), since the two are easy to conflate by name alone.
- **A naming-convention pass, prompted by the observation that the database mixes two kinds of table (what a person asked one pipeline stage to run, vs. what actually got discovered/produced) with no consistent way to tell which is which by name alone.** The four per-stage run-parameter tables already consistently ended in `_runs` (`search_repos_runs`, `inspect_repo_runs`, `inspect_commits_runs`, `classify_bugs_runs`); the actual confusion was two join/output tables that also had the bare word "run" in their name but in a different position and meaning (`run_commits`, `search_run_repositories`), so they could be mistaken for run-parameter tables at a glance despite being catalog data. Renamed both to be prefixed with their full parent run table's name instead: `run_commits` → `inspect_repo_run_commits`, `search_run_repositories` → `search_repos_run_repositories`. No column or behavior changes, pure rename, updated everywhere: `schema.sql`, every SQL string in `Store.scala`/`SearchRepos.scala`/`QualifyRepos.scala`, every doc comment referencing the old names, and every test's raw SQL. Also caught and fixed two schema-comment blocks on `repositories` that had gone stale from the redundancy-audit Fix 1 earlier this session (still describing `current_*` as "qualify-repos's own snapshot" when it's now written by both `search-repos` and `qualify-repos`), while already in that part of the file. Ran the full 37-test suite afterward (not just the directly-touched files) given how many files this rename spanned; all passed.
- **Removed `inspect-repo`'s `validate` mode entirely, folding URL validation into the `list-remotes` clone attempt itself.** The user's observation: a separate `--mode validate` GitHub API call before ever cloning was validating something the very next step (cloning) would fail loudly on anyway, so it was a redundant round trip, not a distinct safety step. `openOrClone`'s `Git.cloneRepository().call()` is now wrapped in a try/catch that turns any clone failure (bad URL, nonexistent repo, network error) into a clean `ERROR: Could not clone <url>: ...` line instead of a raw JGit stack trace, so `list-remotes` failing is itself the validation signal an orchestrator can act on. `InspectRepo` is down to four modes; `Causeway.scala`'s usage text and `InspectRepo`'s own doc comment updated to match.
- **Restructured `/run_causeway` from "ask for a URL and a window, then stop and point at `/inspect_repo`" into "run the entire pipeline, repo URL through classified bug fixes, in one command."** Raised directly by the user: URL gathering and window selection didn't need their own separate command anymore once `/inspect_repo` could own both itself (it already had a fallback path for exactly this, gathering them inline when neither was already known from an earlier command in the session), and a single command that runs the whole thing end to end, asking the same questions along the way without stopping between stages, was worth having as its own entry point. `/inspect_repo` is now the sole owner of gathering the repo URL and window (its old "otherwise, gather them the same way `/run_causeway` does" fallback is now just its own primary path, no more cross-reference to a command that no longer does that); `/run_causeway` now references `/inspect_repo`'s, `/inspect_commits`'s, and `/classify_bugs`'s own numbered steps in sequence (skipping each one's own "find the file from the previous step" step, since the file path is already known, and each one's own final report, folded into one combined report at the end) rather than duplicating their instructions. Fixed the two stale "`/inspect_repo` (or `/run_causeway` if you also want to choose a different time window first)" mentions in `discover_repos.md`/`list_qualifying_repos.md`, which described the old URL-and-window-only `/run_causeway`, not the new whole-pipeline one.
- **Fixed two more stale doc mentions found while touching nearby files, not asked for but caught in passing**: `inspect_repo.md` and `inspect_commits.md` both still said the evidence/enriched files carried "author, committer" data and that storing an enriched file "won't overwrite the author/committer data," both false since author/committer was dropped from the evidence JSON and the `commits` table entirely in an earlier session. Corrected to describe the fields actually written (sha, short/full message, commit date, parent shas).
- **Rewrote `README.md` end to end at the user's request: describe what the workflow does, not why it's built that way, and attach real output rather than illustrative/hand-written examples.** Every "why not X" / "unlike Y" / "the reason this isn't Z" aside was cut from the file-by-file and schema descriptions, keeping only what each piece does. Every example in "The commands, in order" was replaced with output from an actual live run captured while writing this entry: real `search-repos`/`qualify-repos`/`repo-details` output against Java JSON libraries found on GitHub right now, real `list-qualifying-repos` output against the catalog's real accumulated 15 qualifying repositories, and a real end-to-end mine of `stleary/JSON-java`'s last 3 months (28 commits, scan limit 6) through `inspect-repo` → `inspect-commits` → a real `bug-classifier` subagent call on the first batch of 5 commits, correctly identifying the one genuine bug fix among them (closing a real CWE-91 XML-element-injection vulnerability, issue #1071) and rejecting the other four (two release-prep merges, a version bump, a doc link fix). The classified result was written and stored through `tools/causeway store` for real, and both of the SQLite section's SQL examples were re-run live against the resulting database to confirm they still work under the renamed tables and to capture real result rows, rather than hand-typed ones. Also restructured "The commands, in order" and the mermaid diagram around the new `/run_causeway`: the diagram no longer has a separate URL-validation node feeding into `/inspect_repo`, `/inspect_repo` now owns asking for the URL and window itself, with `/run_causeway` shown as an alternate one-command entry into the same chain rather than a distinct upstream step. Verified the full 37-test suite still passed after all the code-level changes this entry covers, then cleared the verification database (`workspace/causeway.db`) afterward.
- **A follow-up readability pass on `README.md`, after the user pointed out it read as confusing: more bullets and tables instead of dense paragraphs, don't assume the reader already knows what's going on, and the `/run_causeway` walkthrough specifically shouldn't read as one compressed "one-shot" transcript.** Added a "pipeline at a glance" table up front (stage → command → input → output) before any deep-dive content. Converted the file-by-file section's dense paragraphs into bullets/tables throughout (e.g. `InspectRepo`'s four modes are now a table, `qualify-repos`'s checks are now a bulleted list). Restructured the walkthrough into three clearly-sequenced, individually-headed sections, `/inspect_repo` → `/inspect_commits` → `/classify_bugs`, each with its own "what it does, in order" list and its own real output, with explicit call-outs replacing assumed inferences (e.g. "only one remote here, so it's used automatically, no question asked" instead of a bare parenthetical); `/run_causeway` is explained afterward as "the same three stages, chained," not folded into one dense block.
- **Added a full "what's in each table" reference to the SQLite catalog section, after the user asked to explain the different types of tables and what they contain**, rather than leaving that scattered across the file-by-file section and the two narrower "how commits connect to PRs" / "discovered repositories" subsections. Three grouped tables (run tables, catalog tables, join tables), one row per actual table in `schema.sql`, what it records. The file-by-file section's own `schema.sql` bullet was trimmed to a one-line pointer at this new section instead of duplicating the same information in prose.
- **Fixed a GitHub-specific mermaid rendering failure** ("Cannot read properties of undefined (reading 'render')") in the architecture diagram, reported by the user after pushing. GitHub's bundled mermaid renderer is stricter than most and doesn't reliably support the hexagon (`{{}}`), double-bracket subroutine (`[[]]`), or double-circle (`(())`) node shapes the diagram had used for external-system/artifact/agent nodes. Replaced every node with the safest common set (plain rectangles, single rounded nodes, one diamond, one cylinder for the database), keeping every label and edge the same, and stripped stray punctuation (`?`, nested parens) from labels as an extra precaution. Confirmed no exotic shapes remained anywhere in the file via `grep` before pushing again.

## 2026-09-13

- **Confirmed against the professor's original seven-item spec, item by item, when asked directly whether each had actually been delivered.** All seven are done: repository snapshot/window-semantics fix (done first, with a regression test proving it), `bugCap` split into `scanCommitLimit`/`bugTarget`, the unified `causeway` CLI, the SQLite catalog, repository search, cheap pre-clone qualification, and typed issue-to-commit relations (seven types delivered, one more than the six originally named). The one acknowledged gap: `detect-tests`/`materialize-commit`/`build` (and their three catalog tables) don't exist yet, deliberately, exactly because the backbone the other seven items describe needed to land first.
- **Made `InspectRepo` and `EnrichCommits` self-store, closing a consistency gap the user raised directly ("i care about consistency tho")**: `search-repos`/`qualify-repos` already wrote straight to `workspace/causeway.db` with no intermediate JSON file; `InspectRepo`'s `write` mode and `EnrichCommits` instead wrote a JSON file and relied on a separate `tools/causeway store --file ...` invocation (with shape-detection by field presence) to reach the database. Added `Store.storeIntoDatabase(json, filePath): Either[String, String]`, a thin wrapper around the existing `storeInto` that opens the real database, upserts, commits, and reports failure without throwing (a database hiccup shouldn't fail a command whose JSON file already wrote successfully), and had `InspectRepo`'s `write` mode and `EnrichCommits` call it directly, in-process, right after writing their own file. `store`'s generic file-detection now does real work only for `/classify_bugs`, the one stage with no compiled Scala tool of its own to call into (classification happens in the chat layer), plus manual/recovery re-storing of any file by hand. Updated `inspect_repo.md`/`inspect_commits.md` to drop their now-redundant separate "store the file" steps (and `run_causeway.md`'s step cross-references, which shifted by one in each), and `README.md`'s file-by-file and walkthrough sections to match, including real `DATABASE=workspace/causeway.db` output re-captured from an actual live run of both self-storing paths. No test needed updating (`write` mode's database-writing was never covered by a compiled test, only used at the CLI level), but the full 37-test suite was re-run to confirm nothing else regressed.
- **Discovered and deliberately declined to fix a related gap while investigating the above: `EnrichCommits`'s actual diff content (file paths, patches, hunks, `isMergeCommit`) has never been persisted anywhere in the database.** `Store.storeEnriched` upserts a commit's message, its linked PRs/issues, and the relations between them, but never reads the enriched JSON's `diff` object at all, and no table exists that could hold it. Discussed three shapes for closing this (full normalization including a `commit_diff_hunks` table; a lighter `commit_diff_files` table with just paths/change-type, pointing at the enriched JSON for the actual patch; or nothing at all) before the user said not to overcomplicate it. Landed on nothing for now: hunk-level line-range data is fault-localization-flavored, which [[bug-mining-design-reframe]] explicitly says is not this project's deliverable (the symptom-to-fault path is); and even the lighter file-path table would be speculative schema with no current reader, the same objection that already got `test_observations`/`materialized_revisions`/`build_runs`/`commit_parents`/`scanned_commit_count` removed earlier. The diff data stays exactly where it already lives, the enriched JSON file, revisit only if a concrete query need shows up.
- **Added `list-runs`, a new pure-database-read subcommand, after the user pointed out a real gap: nothing found "which JSON file to use" by asking the database, despite the database already recording exactly that.** Every evidence/enriched/classified file lands in one flat `workspace/exports/` folder named only `run_<uuid>...json`, no repo, no owner, no window anywhere in the name. `/inspect_commits`/`/classify_bugs`'s fallback path (used whenever the file path isn't already known from earlier in the same conversation) previously just listed that directory and asked the user to pick from bare, context-free filenames, sorted by recency, even though `inspect_repo_runs`/`inspect_commits_runs`/`classify_bugs_runs` all already have a `source_file` column recording the exact path, joinable back to `repositories` for owner/repo and to the run's own window. `ListRuns.scala` queries that existing structure (`--stage inspect-repo` joins `inspect_repo_runs` to `repositories`; `--stage inspect-commits` joins `inspect_commits_runs` through `inspect_repo_runs` to `repositories`, as a `LEFT JOIN` since `inspect_commits_runs.inspect_repo_run_id` is nullable and an enrichment run should still list, with `owner`/`repo`/window shown as `?`, rather than silently vanish behind an inner join), printing `owner`/`repo`/`window`/`sourceFile` per run instead of a bare filename. Updated `inspect_commits.md`/`classify_bugs.md`'s fallback file-picking steps to call this instead of listing the directory, with AskUserQuestion options now labeled `owner/repo (window)` instead of a UUID filename. Added `ListRunsSpec` (3 tests, including one proving the `LEFT JOIN` actually matters: temporarily changed it to an inner join, confirmed the orphaned-enrichment-run test failed by returning nothing instead of a `?`-labeled row, restored the fix, confirmed all 3 passed again). Live-verified against a real `stleary/JSON-java` mining run: `list-runs --stage inspect-repo`/`--stage inspect-commits` both correctly showed `owner=stleary repo=JSON-java window=past-3-months` alongside the real `sourceFile` path. Full 40-test suite passed; verification database cleared afterward.

## 2026-09-15

- **Removed a real duplication between `discover_repos.md` and the `repo-discovery` agent, caught by the user reading `discover_repos.md` in the IDE.** `discover_repos.md` carried its own prose list of search dimensions ("language, how popular, how many times it's been forked, ..."), the exact same taxonomy `repo-discovery.md`'s flag table already owns, maintained in two places (confirmed by recent history: adding the fork-count/repo-age flags a few days earlier required editing both files to stay in sync). Traced through what the orchestrator actually did with that list and found it was dead weight, `discover_repos.md` hands the person's raw plain-language answer to the agent verbatim, the dimension list was never used to construct anything, only to phrase the free-text question. Deleted the sentence; `repo-discovery.md` stays the single source of truth for what search dimensions exist.
- **Closed a second, related gap surfaced while tracing the first one: the orchestrator's results table depended on the `repo-discovery` agent's own free-text report for `stars`/`language`/`sizeKb`, which was never actually guaranteed to be there.** `repo-discovery.md`'s old "Reporting back" instructions said to give "a short list of the results... you don't need to repeat every field", meaning `sizeKb` in particular could silently go missing from what the orchestrator needed for its table. Fixed at the source: `QualifyRepos.scala`'s `QUALIFY` print line already had `stars`/`language`/`sizeKb` in memory (the same `current_*` fields it writes to the catalog), it just never printed them; added them to the line. `repo-discovery.md`'s "Reporting back" section was cut down to a fixed, minimal contract, `SEARCH_RUN_ID`, the flags used, `RESULT_COUNT`, success/failure, nothing about the results themselves, since the orchestrator no longer needs any of that from the agent. `discover_repos.md`'s results-table step now sources `stars`/`language`/`sizeKb` from its own direct `qualify-repos` call instead of "the agent's reading." Net effect matches what the user actually wanted: the orchestrator running `/discover_repos` passes along the raw prompt and the result-count, invokes the agent, and only ever reads back a run id, the flags used, and a count, it never needs domain knowledge about what a repository search dimension even is. Live-verified the new `QUALIFY` line against a real search (`language:Java topic:json min-stars:1000`, 5 real repos), confirmed `stars`/`language`/`sizeKb` printed correctly and matched the corresponding `search-repos` output, and refreshed the README's real-output example with this run rather than leaving the old (pre-fix) output in place. Full 40-test suite passed; verification database cleared afterward.
